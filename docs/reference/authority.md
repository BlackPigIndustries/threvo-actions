# Authority evidence

`AuthorityDecision.DENY` is the preferred denial spelling. It is an additive
alias of `AuthorityDecision.REJECT`; both retain the existing `"reject"` wire
value so stored evidence, commitments, and callbacks remain compatible.

::: threvo_actions.authority
    options:
      members: true
      show_source: false
