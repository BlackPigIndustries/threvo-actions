# AWS KMS protection

!!! note "Optional dependency and release status"

    This API is included in the `0.2.0` release. From the reviewed source
    checkout, install its locked dependencies with
    `uv sync --extra aws-kms --locked`. Use the exact-version command in the
    [installation guide](../getting-started/installation.md#optional-integrations)
    only after the signed release workflow completes.

::: threvo_actions.integrations.aws_kms
    options:
      members: true
      show_source: false
