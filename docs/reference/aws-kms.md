# AWS KMS protection

!!! note "Optional dependency and release status"

    This API is unreleased. From the reviewed source checkout, install its
    locked dependencies with `uv sync --extra aws-kms --locked`. The immutable
    `0.1.4` release does not contain this extra; follow the
    [installation guide](../getting-started/installation.md#optional-integrations)
    for current packaged availability before using a PyPI install command.

::: threvo_actions.integrations.aws_kms
    options:
      members: true
      show_source: false
